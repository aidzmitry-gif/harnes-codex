# HRE-ASTRA-001

Authorization: user requested immediate proven Harness improvements from the supplied Astra article.
Evidence: Goal Runner loaded unrelated update-watcher instructions for every Goal; AGENTS required a full cycle for any edit although ENGINEERING_LOOP limited work items to nontrivial tasks.
Scope: precise skill trigger, conditional radar reference, strictly bounded trivial-prose route. No changes to roles, models, approvals, executable gates or production.
Reuse: existing reference structure and architecture validator. Radar paragraph content preserved except its heading and section link. Existing safety boundaries retained.
Acceptance: source diff review; architecture validates relocated rules and link; existing postchange/release checks pass. Static context-size reduction only, no claim of measured model cost or quality.
Rollback: restore only report before/ preimages and remove the newly introduced radar reference.

Completion: all three gates exit 0; relocated policy equality verified; diff reviewed; installed skill junction verified. See CRM ERP/reports/astra-instructions-20260911/RESULT.md. Prior acceptance receipts are not asserted fresh.
